The full include-list for src/edit.cpp:
#include <fcntl.h>              // for open, O_RDONLY
#include <ncurses.h>            // for endwin, ERR, getch
#include <unistd.h>             // for close
#include <algorithm>            // for max
#include <cstring>              // for strncmp, size_t
#include <iostream>             // for char_traits, basic_istream, cin
#include <string>               // for basic_string, allocator, string, getline
#include <vector>               // for vector
#include "keybinds/sendKeys.h"  // for sendKeys
#include "util/helper.h"        // for calcWindowBounds, centerFileExplorer
#include "util/render.h"        // for renderScreen, initTerminal
#include "util/state.h"         // for State
---
The full include-list for src/util/fileExplorerNode.h:
#include <filesystem>  // for path
#include <string>      // for basic_string, string
#include <vector>      // for vector
---
The full include-list for src/util/fileExplorerNode.cpp:
#include "fileExplorerNode.h"
#include <algorithm>  // for max, sort, copy
#include <sstream>    // for basic_istream, basic_ios, basic_stringstream
#include <stack>      // for stack
---
The full include-list for src/util/query.h:
#include <string>  // for string
struct Query;
---
The full include-list for src/util/query.cpp:
#include "query.h"
#include <string>        // for basic_string, allocator, string
#include "clipboard.h"   // for getFromClipboard
#include "util/state.h"  // for Query
---
The full include-list for src/util/helper.h:
#include <cstddef>       // for size_t
#include <filesystem>    // for path
#include <string>        // for string
#include <vector>        // for vector
#include "state.h"       // for WordPosition, Position, grepMatch
#include "visualType.h"  // for VisualType
---
The full include-list for src/util/helper.cpp:
#include "helper.h"
#include <bits/types/struct_FILE.h>  // for _IO_FILE
#include <curses.h>                  // for stdscr, getmaxyx
#include <sys/types.h>               // for ssize_t
#include <algorithm>                 // for max, transform, find_if, sort
#include <cctype>                    // for isdigit, tolower, isspace, isalnum
#include <cstdio>                    // for size_t, FILE, popen, fgets, pclose
#include <cstdlib>                   // for system, free, NULL
#include <exception>                 // for exception
#include <fstream>                   // for basic_ostream, basic_istream
#include <future>                    // for future, async, launch
#include <map>                       // for map, operator!=, _Rb_tree_iterator
#include <memory>                    // for unique_ptr
#include <sstream>                   // for basic_stringstream
#include <stdexcept>                 // for runtime_error
#include <string>                    // for basic_string, allocator, char_tr...
#include <utility>                   // for pair
#include <vector>                    // for vector
#include "comment.h"                 // for trimLeadingComment
#include "state.h"                   // for State, grepMatch, Position, Query
#include "util/fileExplorerNode.h"   // for FileExplorerNode
#include "util/modes.h"              // for Mode
#include "visualType.h"              // for VisualType
---
The full include-list for src/util/name.h:
#include <string>  // for string
class State;
---
The full include-list for src/util/name.cpp:
#include "name.h"
#include <curses.h>       // for ERR, KEY_BACKSPACE, KEY_LEFT, KEY_RIGHT, getch
#include "util/helper.h"  // for ctrl
#include "util/modes.h"   // for Mode
#include "util/query.h"   // for backspaceAll, add, addFromClipboard, backspace
#include "util/render.h"  // for renderScreen
#include "util/state.h"   // for State, Query
---
The full include-list for src/util/indent.h:
#include <string>  // for string
class State;
---
The full include-list for src/util/indent.cpp:
#include "indent.h"
#include <stddef.h>      // for size_t
#include <string>        // for basic_string, allocator, char_traits, operat...
#include <vector>        // for vector
#include "comment.h"     // for trimComment
#include "helper.h"      // for ltrim, getExtension, safeSubstring
#include "util/state.h"  // for State, Position
---
The full include-list for src/util/insertLoggingCode.h:
#include <string>  // for string
class State;
---
The full include-list for src/util/insertLoggingCode.cpp:
#include "insertLoggingCode.h"
#include <stddef.h>      // for size_t
#include <algorithm>     // for max
#include <regex>         // for regex_search, regex
#include <string>        // for basic_string, allocator, char_traits, operat...
#include <vector>        // for vector
#include "helper.h"      // for getExtension, ltrim
#include "indent.h"      // for indentLine
#include "util/state.h"  // for State
---
The full include-list for src/util/comment.h:
#include <string>  // for string
class State;
struct Bounds;
---
The full include-list for src/util/comment.cpp:
#include "comment.h"
#include <stddef.h>  // for size_t
#include <climits>   // for INT_MAX
#include <string>    // for basic_string, allocator, operator+, operator==
#include <vector>    // for vector
#include "bounds.h"  // for Bounds
#include "helper.h"  // for ltrim, rtrim
#include "indent.h"  // for getNumLeadingSpaces
#include "state.h"   // for State
---
The full include-list for src/util/state.h:
#include <filesystem>    // for path
#include <string>        // for basic_string, string
#include <vector>        // for vector
#include "modes.h"       // for Mode
#include "visualType.h"  // for VisualType
class FileExplorerNode;
---
The full include-list for src/util/state.cpp:
#include "state.h"
#include <curses.h>                 // for endwin
#include <stdlib.h>                 // for exit
#include <algorithm>                // for max
#include <iostream>                 // for basic_ostream, operator<<, cout
#include <string>                   // for basic_string, allocator, string
#include <vector>                   // for vector
#include "helper.h"                 // for getCommentSymbol, readFile, cente...
#include "modes.h"                  // for Mode
#include "util/fileExplorerNode.h"  // for FileExplorerNode
#include "visualType.h"             // for VisualType
---
The full include-list for src/util/render.h:
#include <string>  // for string
class State;
---
The full include-list for src/util/render.cpp:
#include "render.h"
#include <ncurses.h>                // for init_pair, COLOR_PAIR, move, mvpr...
#include <stdlib.h>                 // for exit, size_t, NULL
#include <filesystem>               // for path
#include <iostream>                 // for basic_istream, basic_ostream, ope...
#include <regex>                    // for regex_search, regex
#include <sstream>                  // for basic_stringstream
#include <string>                   // for basic_string, allocator, char_traits
#include <vector>                   // for vector
#include "helper.h"                 // for getLineNumberOffset, unctrl, getE...
#include "insertLoggingCode.h"      // for getLoggingRegex
#include "modes.h"                  // for Mode
#include "state.h"                  // for State, Query, Position, grepMatch
#include "util/fileExplorerNode.h"  // for FileExplorerNode
#include "util/visualType.h"        // for VisualType
---
The full include-list for src/util/clipboard.h:
#include <string>  // for string
class State;
---
The full include-list for src/util/clipboard.cpp:
#include "clipboard.h"
#include <stdio.h>                       // for pclose, popen, FILE, fgets
#include <algorithm>                     // for max
#include <array>                         // for array
#include <exception>                     // for exception
#include <filesystem>                    // for path, copy_options, canonical
#include <sstream>                       // for basic_istream, basic_strings...
#include <stdexcept>                     // for runtime_error
#include <string>                        // for basic_string, string, char_t...
#include <vector>                        // for vector
#include "../keybinds/sendVisualKeys.h"  // for deleteInVisual, changeInVisual
#include "helper.h"                      // for fixColOverMax, getUniqueFile...
#include "indent.h"                      // for indentLine
#include "state.h"                       // for State, Position
#include "util/visualType.h"             // for VisualType
---
The full include-list for src/keybinds/sendShortcutKeys.h:
class State;
---
The full include-list for src/keybinds/sendShortcutKeys.cpp:
#include "sendShortcutKeys.h"
#include <ncurses.h>                    // for KEY_BACKSPACE, KEY_DOWN, KEY_...
#include <algorithm>                    // for max
#include <cctype>                       // for tolower, toupper, islower
#include <exception>                    // for exception
#include <filesystem>                   // for is_regular_file, path, exists
#include <string>                       // for basic_string, allocator, char...
#include <vector>                       // for vector
#include "../util/clipboard.h"          // for copyToClipboard, pasteFromCli...
#include "../util/comment.h"            // for toggleComment, unCommentBlock
#include "../util/helper.h"             // for ctrl, initVisual, centerScreen
#include "../util/history.h"            // for applyDiff
#include "../util/indent.h"             // for indentLine
#include "../util/insertLoggingCode.h"  // for toggleLoggingCode, removeAllL...
#include "../util/modes.h"              // for Mode
#include "../util/query.h"              // for setQuery, backspaceAll, add
#include "../util/render.h"             // for renderScreen
#include "../util/state.h"              // for State, diffLine, Position
#include "../util/visualType.h"         // for VisualType
#include "sendKeys.h"                   // for sendKeys
#include "sendVisualKeys.h"             // for getInVisual, sendVisualKeys
#include "util/fileExplorerNode.h"      // for FileExplorerNode
---
The full include-list for src/keybinds/sendFindFileKeys.h:
class State;
---
The full include-list for src/keybinds/sendFindFileKeys.cpp:
#include "sendFindFileKeys.h"
#include <ncurses.h>            // for KEY_BACKSPACE, KEY_DOWN, KEY_LEFT
#include <filesystem>           // for path, relative
#include <string>               // for basic_string
#include <vector>               // for vector
#include "../util/clipboard.h"  // for copyToClipboard
#include "../util/helper.h"     // for ctrl, generateFindFileOutput, isTestFile
#include "../util/modes.h"      // for Mode
#include "../util/query.h"      // for backspaceAll, add, addFromClipboard
#include "../util/render.h"     // for renderScreen
#include "../util/state.h"      // for State, Query
---
The full include-list for src/keybinds/sendKeys.h:
class State;
---
The full include-list for src/keybinds/sendKeys.cpp:
#include "sendKeys.h"
#include <curses.h>                // for endwin
#include <stdlib.h>                // for exit
#include <algorithm>               // for max
#include <string>                  // for basic_string, allocator, char_traits
#include <vector>                  // for vector
#include "../util/helper.h"        // for ctrl, calcWindowBounds, centerScreen
#include "../util/history.h"       // for generateDiff
#include "../util/modes.h"         // for Mode
#include "../util/state.h"         // for State, Position, diffLine, Jumplist
#include "sendBlameKeys.h"         // for sendBlameKeys
#include "sendCommandLineKeys.h"   // for sendCommandLineKeys
#include "sendFileExplorerKeys.h"  // for sendFileExplorerKeys
#include "sendFindFileKeys.h"      // for sendFindFileKeys
#include "sendGrepKeys.h"          // for sendGrepKeys
#include "sendMultiCursorKeys.h"   // for sendMultiCursorKeys
#include "sendSearchKeys.h"        // for sendSearchKeys
#include "sendShortcutKeys.h"      // for sendShortcutKeys
#include "sendTypingKeys.h"        // for sendTypingKeys
#include "sendVisualKeys.h"        // for sendVisualKeys
---
The full include-list for src/keybinds/sendCommandLineKeys.h:
class State;
---
The full include-list for src/keybinds/sendCommandLineKeys.cpp:
#include "sendCommandLineKeys.h"
#include <ncurses.h>         // for endwin, KEY_BACKSPACE, KEY_LEFT, KEY_RIGHT
#include <stdlib.h>          // for exit
#include <algorithm>         // for max
#include <sstream>           // for basic_istream, basic_istringstream, istr...
#include <string>            // for basic_string, operator==, allocator, get...
#include <vector>            // for vector
#include "../util/helper.h"  // for ctrl, saveFile, trimTrailingWhitespace
#include "../util/modes.h"   // for Mode
#include "../util/query.h"   // for backspaceAll, add, addFromClipboard, bac...
#include "../util/render.h"  // for renderScreen
#include "../util/state.h"   // for State, Query
---
The full include-list for src/keybinds/sendMultiCursorKeys.h:
class State;
---
The full include-list for src/keybinds/sendMultiCursorKeys.cpp:
#include "sendMultiCursorKeys.h"
#include <curses.h>          // for KEY_BACKSPACE
#include <string>            // for basic_string, allocator, operator+, char...
#include <vector>            // for vector
#include "../util/bounds.h"  // for Bounds
#include "../util/helper.h"  // for safeSubstring, ctrl, autocomplete, getCu...
#include "../util/modes.h"   // for Mode
#include "../util/state.h"   // for State
#include "sendVisualKeys.h"  // for getBounds
---
