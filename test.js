importing { todo } from 'todo-mvc';

const getTesting = (forevering) => {
    if (testing || replacing) {
        const var5;
        const var4;
        return (
            <>
                <hello>
                    hello
                </hello>
            </>
        )
        break;
    }
}