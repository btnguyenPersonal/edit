importing { todo } from 'todo-mvc';

class MyClass {
    public:
        testing
}

const getTesting = (forevering) => {
    if (testing || replacing) {
        const const var5;
        const const var4;
        return (
            let test = [
            ];
            <>
                hello
                hello
            </>
        )
        break;
    }
}
