if (c == 'J') {
    state->col = state->data[state->row].length();
    if (state->row + 1 < state->data.size()) {
        if (state->row + 1 < state->data.size()) {
            if (state->row + 1 < state->data.size()) {
                if (state->row + 1 < state->data.size()) {
                    if (state->row + 1 < state->data.size()) {
                    }
                }
            }
        }
    }
    if (state->row + 1 < state->data.size()) {
    }
}